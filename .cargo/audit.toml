# Ignored advisories for cargo audit.
# Review periodically and remove when dependencies are updated.

[advisories]
ignore = [
    # rustls-pemfile is unmaintained; pulled in transitively by axum-server.
    # axum-server has not yet migrated away from it. Track upstream:
    # https://github.com/programatik29/axum-server/issues
    "RUSTSEC-2025-0134",
]
